# AI API Configuration
# OpenAI is used for Whisper transcription
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic Claude Opus 4 is used for matching tweets to videos
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Environment
NODE_ENV=production

# Optional: Cleanup endpoint security
CLEANUP_SECRET=your-secret-key-for-cleanup

# Optional: Custom paths (Railway will auto-detect)
# FFMPEG_PATH=/usr/bin/ffmpeg
# YTDLP_PATH=/usr/local/bin/yt-dlp

# Optional: Rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=10

# Optional: Max video duration in seconds (default: 3600 = 1 hour)
MAX_VIDEO_DURATION=3600

# Optional: Max concurrent downloads
MAX_CONCURRENT_DOWNLOADS=3 